import express from 'express';

/**
 * Route namespace to contain all Lidarr related routes and handlers.
 */
export namespace Route {
    /**
     * ROUTING AND HANDLERS
     */
    export const router = express.Router();
}
